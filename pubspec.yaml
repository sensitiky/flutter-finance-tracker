name: fundora
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  provider: ^6.1.2
  logging: ^1.3.0
  fl_chart: ^0.69.0
  local_auth: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.2

flutter_native_splash:
  color: "#ffffff"
  image: "assets/img/fundora.png"

flutter:
  uses-material-design: true
  assets:
    - assets/img/
